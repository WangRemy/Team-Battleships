from PyQt4 import QtCore, QtGui



		
""" connect selected coordinate to grid
	for x in range(1,10):
		for y in range(1,10):
			self.coord = self.gameui.findChild(QtGui.QPushButton, "s" + str(x) + str(y))
			#self.gameui.coord.clicked.connect(lambda: self.choosecoords(self.coord, self.woordenlijst))
			#Note the lambda will avoid the evaluation of the function call, so it'll call self.choosecoords(self.coord, self.woordenlijst) only when clicked
"""
